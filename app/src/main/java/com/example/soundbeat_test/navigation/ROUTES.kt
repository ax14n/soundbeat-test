package com.example.soundbeat_test.navigation

/**
 * Objeto que contiene las rutas de navegación utilizadas en la aplicación.
 *
 * Estas constantes definen los nombres de las pantallas o destinos que
 * se utilizarán en el sistema de navegación, como por ejemplo con Jetpack Navigation
 * o cualquier otro sistema basado en rutas.
 */
object ROUTES {
    const val LOGIN = "LOGIN"
    const val REGISTER = "REGISTER"
    const val HOME = "HOME"
    const val SETTINGS = "SETTINGS"
    const val SELECTED_PLAYLIST = "SELECTED_PLAYLIST"
}